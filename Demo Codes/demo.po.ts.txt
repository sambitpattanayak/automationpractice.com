import { browser, by, element, $, $$ } from 'protractor';
export class DemoPage {
  /**
   * @name launchapp
   * @description it will lauch application in browser
   * @returns promise
   */
  async launchapp () {
    await browser.driver.get('https://juliemr.github.io/protractor-demo/'); // syntax to open a website
    await console.log('Application launched successfully');
  }
}

